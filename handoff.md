# Handoff — ESSPN / Crown League

_Last updated: 2026-07-08 · **V1 shipped & LIVE**. Soccer engine feature-complete. **🏉 THE RUGBY MATCH ENGINE IS NOW BUILT AND VERIFIED** — the operator locked **rugby union**, and the full deterministic union sim → choreographer → continuous-play renderer → MP4 export chain shipped in one pass: calibrated scorelines (~50 pts, ~5.8 tries, 75% conversions, 2% draws), sin-bin/red-card send-offs (yellows walk back on), penalty corners → lineout mauls, try groundings + conversions off the tee, Bastion broadcast overlay, and a live Rugby tab (pick clubs → watch → export the Reel). 126 tests green (79 soccer untouched + 47 rugby), production build clean, in-browser export smoke passed (81MB H.264/AAC MP4). **Next: plug rugby into the league/persistence/content spine (Bastion league mode with rugby standings).**_

**Full-frame stadium layout (2026-07-08):** the old top/bottom dead space is gone — wordmark+scorebug pulled up tight (y 95 / 150), the pitch grown to fill 384-1776 (w:h 0.747), deep 5-row tiered crowd terraces (front-to-back size/brightness falloff, away section rotating through the four goal-end corners per match) replacing the thin rails, and **goal frames + nets** at each end so a ball in the top net lands in a real goal with the terrace erupting behind it. Money shot preserved (ball y~363, clear of scorebug bottom 250). 4 layout regression tests lock the fill + clearance geometry.

## 🎯 Goals
V1 is done and live; the Soccer engine is feature-complete; **the Rugby match engine is built** — the bet paid off: the deterministic-sim → continuous-play-renderer shape generalized cleanly (every rugby module is an isolated mirror of its soccer counterpart, zero soccer files modified). The active goal is now to **plug rugby into the league spine**: a persistent Bastion Championships season (rugby-points standings — likely 4 pts win / 2 draw + bonus points, operator to confirm), fixtures, matchday content packs, and a League-style tab flow.

## 📍 Current State — LIVE at frankyface.github.io/SportsSims
- **Live on GitHub Pages** (Pages Source = "GitHub Actions"; deploys on every push).
- **Brand:** ESSPN network wordmark **E·SS·PN** "presents" its competitions — the **Crown League** (Soccer) and now the **Bastion Championships** (Rugby). Real club crests + competition logos live in `src/assets/logos/`.
- **Rating model (Elo/Glicko-2, `src/ratings/`):** starting ratings are a **random draw — NOT tied to a club's archetype**; every new league uses a **random seed**. Ratings **carry over season-to-season with a small offseason drift**; a **"big offseason" raises volatility**. No regression to the mean. See `offseasonAdjust` in `glicko2.ts` + `advanceSeason` in `league.ts`.
- **Match sim (`src/sim/`):** deterministic possession/xG/momentum, calibrated (~2.9 goals, ~26 shots, ~23% draws). Determinism hygiene enforced (no transcendental math / `Math.random` / `**` in the sim). **SIM_VERSION 4**: records **possession spans** for the continuous-play renderer AND applies a **red-card handicap** (shorthanded possession/creation/defense multipliers). The score-deciding RNG stream structure is frozen (`scoreCompat.test.ts` golden snapshot): v2→v3 byte-identical everywhere; v3→v4 byte-identical for every match WITHOUT a red card (red-card matches deliberately play out differently from the card onward — stored league scores unaffected). A **choreographer** (`src/sim/choreographer.ts`, own `':pbp'` PRNG stream + shared formation table `src/sim/formation.ts`) expands spans into unbroken touch chains: passes between player slots, carries, risky through-balls, interceptions, shots/saves/misses, restarts. **Soccer only — no rugby sim yet.**
- **Video (`src/render/`, `src/export/`):** **CONTINUOUS PLAY** (the old beat-based highlight reel is gone). The director (`director.ts`) lays the touch chains on the render clock — the scorebug **clock counts up 0'→90'** in its own chip (fast through quiet spells, near real-time in big moments), the **score steps live on goals**, and `moments` drive lower-thirds (goals, big chances, saves, misses, cards incl. card chip, half-time) + audio cues. The scene (`renderScene.ts`) moves players every frame: formation base + possession push/compress + **runs to meet passes** + keeper ball-tracking + goal-celebration swarms; the ball travels every segment with arc/streak/interception-ping visuals — **no teleports** (distance-proportional pacing, machine-checked <160px/frame @30fps). Pitch sits below the scorebug so top-net goals stay visible. `drawFrame` stays a pure fn of `(model, t)` → preview & WebCodecs MP4 (1080×1920 H.264/AAC) stay identical; clips run **~55-69s** (square-race length), with storyline captions, corner set-pieces, send-off walk-offs, goal confetti + shake, and **crowd stands behind each goal** (home-coloured ends + a segregated away section that leaps when its team scores; HOME/AWAY tags on the scorebug). Audio is **side-aware and ducked**: two ~74s stadium ambience beds rotate across matches (each plays once from a seeded offset — no loop-repeat), and goal cheers / boos (real recordings) briefly **duck** the crowd bed so they cut through (~4.5× lift at goals, verified); home goals roar, away goals get the travelling-section cheer + home boos, cards draw jeers. The **matchday intro card** frames the two clubs with the Crown League crest centred; clubs without a crest render a **colour-badge fallback** (`drawCrestChip`).
- **League (`src/league/`):** 6-team double round-robin (30 games, 10 matchdays), **top-4 playoffs**, champion, offseason rollover. Persists to localStorage + optional GitHub repo (Contents API, token in Settings).
- **Content (`src/content/`):** one-click **matchday content pack** + **Download Season Content** (zip, in posting order).
- **UI (`src/ui/`):** tabbed app — **League / Friendly / Rugby / Settings**.
- **🏉 Rugby (Bastion Championships) — MATCH ENGINE LIVE (union), friendly-tab flow shipped:**
  - **Deterministic union sim** (`src/sim/rugbySim.ts` + `rugbyTypes.ts`, `RUGBY_SIM_VERSION 1`): phase-based possessions → tries (5) + conversions (2, central-grounding difficulty × kicker boot), penalty awards (kick at goal vs corner → lineout-drive tries/held-up), drop goals, **sin-bin yellows (10 sim-min shorthanded) + permanent reds**, momentum + comeback rubber-band. Calibrated Monte-Carlo (N=3000): ~50.5 pts, ~5.8 tries, 74.8% conversions, ~4.1 pen goals, 2.1% draws, ~1.2 yellows; red before 60' → loss rate 64% vs 50% (N=6000). Own frozen stream, golden-guarded by `rugbyScoreCompat.test.ts`; hygiene scan covers the rugby files automatically (they live in `src/sim/`).
  - **Rugby choreographer** (`src/sim/rugbyChoreographer.ts` + `rugbyFormation.ts`, 10 dots/side: fullback + pack + backline): carry→ruck→backward-pass phases, kick tennis caught by the fullback, penalty tee routines, corner lineout→maul→grounding, conversions folded into try passages, jackal turnovers. **Passes NEVER travel forward** (machine-enforced). Sin-binned slots untouchable while off; `':pbp'` stream, ball continuity to 9 decimals.
  - **Rugby director/scene/overlay** (`src/render/rugbyDirector.ts`, `rugbyStoryline.ts`, `rugbyScene.ts`, `rugbyRenderMatch.ts`): 80' counting clock, score steps at grounding AND conversion strike, context-aware try labels (STRIKE FIRST / WIN IT LATE / RUNNING RIOT…), rugby storyline captions (14-men, arm wrestle, points fest, early blitz — collision-free + score-invalidated), pitch with in-goals + H posts + 22m/10m lines, flat defensive line + ruck pile-ins + backline depth, sin-bin walk-off AND walk-back, oval ball with flight spin, TRY! flash + confetti, Bastion intro/result cards. Anti-teleport gate <160px/frame verified incl. stress seeds; runtime band 53-70s over 200 seeds.
  - **Export + audio** (`src/export/exportRugbyMp4.ts`): same WebCodecs pipeline; rugby moments adapt onto the shared audio mixer (tries roar like goals, kicked goals cheer, cards draw boos, whistles at kickoff/half/full-time). In-browser smoke: 81MB video/mp4 produced.
  - **Rugby tab** (`src/ui/RugbyTab.tsx` + `RugbyMatchView.tsx`): FriendlyTab-style club pickers + Shuffle + New match (seed `rugby-friendly:<home>:<away>:<gen>`), looping live preview (same pure `drawRugbyFrame` as the export), ⬇ Export Instagram MP4; club book below. Verified in-browser: zero console errors, TRY flash + lower thirds + scorebug all live.
- **Tests: 126 passing** (79 soccer — all untouched — + 47 rugby mirroring every soccer gate: golden score-compat, calibration + red-card Monte-Carlos, choreographer continuity/backward-pass/sin-bin invariants, director clock/scorebug/overlay/label suites, anti-teleport + liveliness + layout money-shot). **Build clean (`npm run build`).**

## 📂 Files
`src/sim` (soccer: simulateMatch, choreographer, formation · **rugby: rugbySim, rugbyTypes, rugbyChoreographer, rugbyFormation**) · `src/ratings` (glicko2, teams, rugbyTeams, strength) · `src/render` (soccer: director, storyline, renderScene, renderMatch · **rugby: rugbyDirector, rugbyStoryline, rugbyScene, rugbyRenderMatch** · standingsCard, wordmark, logos, rugbyLogos) · `src/export` (exportMp4, **exportRugbyMp4**, audio, audioAssets) · `src/league` · `src/content` · `src/ui` (LeagueTab, FriendlyTab, **RugbyTab, RugbyMatchView**, SettingsTab, …) · `src/App.tsx` · `src/assets/logos` (soccer crests + Crown League + 6 rugby crests + bastion).

## ✅ Things I've Changed (newest first)
- **🏉 Rugby v2 — realism overhaul + LEAGUE MODE + tab restructure (operator feedback, same day):**
  - **Realism (operator: "it doesn't look like rugby"):** player dots 27→21px; whole game slowed (play window 58-74s vs 48-62, SPEED_FLOOR 1.15, heavier per-beat dwell); passes now travel VISIBLY backward (0.03-0.09 of the pitch, machine-checked: >60% of passes are clearly deep); kicks get real hang time (w×1.4, 1.9× flight scale, trajectory intent line) so they never read as forward passes; **the onside law is rendered AND tested** — while a kick is in the air the kicking team retreats behind the boot (only the named chaser goes); the phase engine is a weighted menu of real shapes: **forward pods off the ruck, backline sweeps (each pass deeper), switch/scissors plays, offload chains through contact, chips/box kicks to space chased by the winger from deep** (all quota-tested in rugbyChoreographer.test).
  - **League mode (`src/league/rugbyLeague.ts` + tests):** persistent Bastion Championships — UNION standings (4 win / 2 draw / 0 loss, +1 four-try bonus, +1 losing-within-7 bonus, ranked pts→PD→PF; math unit-tested), double round-robin via shared generateFixtures, top-4 playoffs (sf1/sf2/final, ties to seed), Glicko offseason with big-offseason volatility, own localStorage namespace (`elitesim:rugbyleague:bastion-championships`). Deterministic: same seed → identical season, fixtures re-sim for video.
  - **Rugby content packs:** `content/rugbyMatchdayPack.ts` (a video per game + Match of the Week starred via rugby dramaScore + the Bastion standings PNG + rugby captions with `#SimRugby`); `render/rugbyStandingsCard.ts` (P W D L PD BP PTS card). Season-zip download for rugby NOT yet built (soccer-only) — next-session item.
  - **Tab restructure:** top level is now **⚽ Soccer | 🏉 Rugby | Settings**; Soccer holds the existing League+Friendly (unchanged components), Rugby holds League+Friendly+Clubs sub-tabs (`ui/SoccerTab.tsx` new, `ui/RugbyTab.tsx` restructured, `ui/RugbyLeagueTab.tsx`+`RugbyStandingsTable.tsx` new, App.tsx owns both league states).
  - 134 tests green (8 new league + onside/repertoire suites); build clean; verified in-browser (full season simmed: champion crowned, bonus-point table correct, tap-to-watch works).
- **Rugby launch code-review pass (8-angle multi-agent review, same day):** fixed everything that survived verification — tries are now grounded by the player who made the break (the slot picker was excluding the carrier, so a different dot always scored); every card is a MUST-stage moment (a sin-binned player can no longer walk off banner-less when the highlight budget is full); send-off slots draw without replacement (a red-carded dot can never return via a later yellow's slot collision) and anchor to the exact possession end (no more walking off before the foul); try labels are scoreboard-honest (conversion included, later penalty goals respected — no more "WIN IT LATE" on a loss); conversion outcome is a moment KIND (`conversion`/`conversionMiss`), killing the label-sniffing in the audio adapter, which is now cast-free; the late-drama caption requires earned closeness (soccer's activity gate, ported); receiver chains reset per passage (no cross-team slot exclusion leak); sim judges shorthandedness at possession START and possession % sums to 100 (golden regenerated same-day pre-release — decision documented in rugbyScoreCompat.test.ts; RUGBY_SIM_VERSION stays 1; calibration unchanged). Not changed, documented: team-level sin-bin merge on overlapping yellows (rare, v1 simplification, noted in rugbySim.ts).
- **🏉 THE RUGBY MATCH ENGINE (union)** — operator locked union; built the full chain in isolated rugby modules mirroring the soccer blueprint: `sim/rugbyTypes+rugbySim` (calibrated deterministic sim, sin-bin/red mechanics, frozen v1 stream + golden), `sim/rugbyFormation+rugbyChoreographer` (phases/rucks/backward passes/kick tennis/corner mauls/tee routines; anti-teleport by construction), `render/rugbyDirector+rugbyStoryline+rugbyScene+rugbyRenderMatch` (80' clock, try/conversion score steps, rugby captions, in-goal pitch + H posts, defensive line + ruck pile-ins, sin-bin walk-off/return, TRY! flash, Bastion cards), `export/exportRugbyMp4` (WebCodecs + shared audio via moment-kind adapter), `ui/RugbyTab+RugbyMatchView` (pick → watch → export). 47 new tests; zero soccer files touched (only additive: one CSS class). Verified: 126/126 green, clean build, browser preview error-free, 81MB MP4 export smoke.
- **Audio finalized:** two new **rotating** stadium ambience beds (Brazil steady + crowd audiences, ~74s each, seeded rotation, play-once so nothing repeats mid-clip); **broadcast ducking** so goal cheers/boos cut through the bed (median ~4.5× lift, verified, no clipping); fixed `cheer-2` (had a ~2s build-up → re-trimmed to roar on the strike). Earlier same-session: continuous non-looping ambience (killed the repeated-kids-scream), darkened synth bed (65% less HF churn), single (un-doubled) boos.
- **QoL wave:** (1) **Friendly tab** now has home/away **dropdowns + Shuffle**, and every "New match" bumps a `gen` counter into the seed so the SAME matchup replays differently each time (was: 3 fixed pairings on repeat). (2) **Crowd bed darkened** — cascaded 2-pole low-pass, 65% less HF churn (a murmur, not static). (3) **Match of the Week**: `content/matchOfWeek.ts` `dramaScore()` picks the standout game per round; labelled in season-pack folders `(MATCH OF THE WEEK)`, captions (⭐), POSTING_ORDER, and matchday filenames. (4) **Away crowd section rotates** through all four goal-end corners per match (seeded). (5) **Boos no longer double** — `boo-2/3` regenerated as single groans (were crossfade self-loops).
- **Full-frame stadium layout:** wordmark/scorebug pulled up tight, pitch grown to 384-1776, deep tiered crowd terraces + goal frames/nets (money shot preserved). 4 layout regression tests.
- **Real crowd audio (LIVE):** operator's cheer/boo/ambience recordings, 48kHz mono WAV, side-aware mix.
- **Real rules (SIM_VERSION 4):** red cards now **send a player off** (walk-off animation, team plays with 7 dots, sent-off slot never touches the ball) AND **handicap the ten men** in the sim (possession/creation/defense multipliers → early-red loss rate 52% vs 35% baseline over 6k matches). Multiplier-only change: **no-red matches stay byte-identical to v3**; the golden snapshot was deliberately regenerated (verified: exactly the 5/30 red-containing seeds changed). **Corner kicks** staged deterministically on the ':pbp' stream (tipped-behind saves → corner + risky cross + clearance; byline goals → corner headers) with a "CORNER — ABBR" third. Goal **confetti burst + screen shake**, intro card **pops** (scroll-stopper hook).
- **Square-race pacing + storyline commentator** (~55-69s clips, was ~30-40s): slower ball (~40%), 3-4 passes per bridge, featured dwell 1.4x; deterministic storyline captions (pressure runs, comeback, late drama, dominance irony, red-card response — goal-invalidated, collision-free) + context-aware goal labels (STRIKE FIRST / LEVEL IT / WIN IT LATE / RUNNING RIOT…); receivers meet passes exactly; cliff-free defending gate (press + box collapse); late-game clock pulse. Verified by a 4-lens workflow; all fuzz-found HIGHs fixed + regression-tested.
- **Season pack folders**: `R01.1 - KNG vs COB/` … `SF.1`, `FINAL`, each with `1-game.mp4`, `2-standings.png`, `caption-game.txt`, `caption-standings.txt`; `POSTING_ORDER.txt` at the root.
- **Real crowd audio (LIVE)**: the operator's recordings — `cheer-1/2`, `boo-1/2/3`, `music-1` (ambient bed) — trimmed to 5-7s, 48kHz mono WAV, loudness-matched, mixed side-aware into exports; procedural crowd remains the fallback. Verified: exported MP4 carries a real AAC track (48kHz mono, matches video length). Drop-in system: `src/assets/audio/` discovered by `music-*`/`cheer-*`/`boo-*` name prefix.
- **Continuous-play match engine** (replaces the highlight reel): sim v3 records possession spans (score stream frozen vs v2 — golden-guarded); new `choreographer.ts` + `formation.ts` stage passes/risky balls/interceptions/restarts as one unbroken chain; `director.ts` rewritten (counting clock, live score keyframes, moments); new `renderScene.ts` (moving players, keeper tracking, celebration swarms, ball arcs/streaks/pings); scorebug clock chip; pitch moved below the bug so top-net goals are visible; translucent lower-thirds; audio now cue-driven by moments. 25 new tests incl. anti-teleport gate + overlay-priority + fuzz-found regression seeds; verified in-browser (pixel checks + real 13MB MP4 export smoke).
- **Rugby identity layer:** 6 Bastion Championships clubs (`rugbyTeams.ts` + tests), their real crests + the Bastion logo (downscaled, wired via the isolated `rugbyLogos.ts`), and a read-only **Rugby club-book tab**. Renamed the competition to **Bastion Championships** to match the crest artwork.
- **Restyled the matchday intro card** — the two clubs now frame the card with the Crown League crest centred; added a colour-badge crest fallback for clubs without a logo.
- Club book now uses the real embedded crests + Crown League branding.
- Bigger field (~75%) + 8 players/side; scaled-up UI; **random seed per league**; corrected Meridian crest; logos downscaled.
- **Real club crests + Crown League logo** in videos/standings/club book; league renamed **Crown League**.
- **Decoupled ratings from archetype + offseason carry-over model**; swapped Blackwater Wanderers → **Meridian FC**.
- Rebrand to **ESSPN** + 6 characterful clubs; shrank league to 6 teams (top-4 playoffs).
- **Download Season Content** (zip). Stage 4 matchday pack. Stage 3 persistent league. Stage 2 audio. Stage 1 renderer + WebCodecs export. Elo/Glicko-2 model. Docs scaffold + research.

## ❌ Deferred / Open questions
- ~~Rugby match sim not built~~ / ~~union vs league~~ — **RESOLVED 2026-07-08: operator locked UNION; engine built + verified** (see Current State). Remaining rugby work is the **league-spine plug-in**: rugby standings need their own points scheme (union convention: 4 win / 2 draw / 0 loss, +1 four-try bonus, +1 losing-within-7 bonus — confirm with operator), a `createRugbyLeague`/`simRugbyFixture` mirroring `league.ts` (which is soccer-locked via `MatchScore.xg` + 3/1/0 points), and matchday content packs for rugby.
- Cosmetic crest backgrounds: **Highmoor** ("Stags") crest has a grey *gradient* baked background → shows as a grey square on dark cards; best **regenerated on a transparent/dark background**. **Bastion** logo has a *flat white* background → can be cleanly knocked out to transparent (offered — not yet done).
- ffmpeg.wasm export fallback for non-WebCodecs browsers (operator uses Chrome — deferred).
- Exported MP4s are chunky (~58MB for ~60s — the busy crowd raises the H.264 bitrate). Fine for IG; drop `BITRATE` in `exportMp4.ts` if smaller files are ever wanted.
- **Playoffs are top-4 of 6** — could trim to **top-2 → a one-match Grand Final** (user's call, still open).
- **Cobalt Bay logo is a horizontal wordmark** — circle-crop shows its centre; could be regenerated as a round crest.

## ➡️ Next Up (post-V1)
1. ~~Rugby league mode~~ — **DONE (2026-07-08 v2)**: full Bastion Championships season with union bonus-point standings, playoffs, matchday packs, tap-to-watch. Remaining rugby follow-ups: **season-zip download** (mirror `content/seasonContent.ts` for rugby), and watch operator feedback on the v2 realism pass (pods/switches/offloads/kicks-to-space are in; more shapes — scrums, lineout steals, 50:22s — are natural v3 candidates).
2. Crest polish for rugby overlays: knock out the **Bastion** white background; regenerate **Highmoor** on a transparent bg.
3. **Golf** — heats of 4, majors, season rankings (individual sport).
4. **Stage 6** — star players & drama; public standings page. **Stage 7** — automation ladder.
- Human tasks: `help.md` #3 (save token) to turn on cloud save; #4–6 for eventual auto-posting.

## 🔗 Pointer
→ Current stage folder: `staging/stage-5-rugby-and-golf/`
→ Active feature file: **`staging/stage-5-rugby-and-golf/feature-rugby.md`** — **the rugby match engine (union) is BUILT + VERIFIED**; the remaining open item is the **league-spine plug-in** (rugby standings/points, season UI, content packs). Start there — confirm the union bonus-points scheme with the operator, then mirror `league.ts`'s soccer-locked pieces.
